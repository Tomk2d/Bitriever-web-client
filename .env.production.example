# =============================================================================
# Next.js client — 운영 빌드용 API Base URL
# 사용: 이 파일을 .env.production 으로 복사한 뒤, 실제 운영 백엔드 URL로 수정.
#       next build 시 NODE_ENV=production 이므로 .env.production 이 로드됨.
#       또는 배포 환경( Docker / PM2 / CI )에서 APP_SERVER_URL, NEXT_PUBLIC_API_BASE_URL 전달.
# =============================================================================

# 백엔드 API 주소 (OAuth 리다이렉트, API 프록시 등)
APP_SERVER_URL=https://api.bitreiver.com

# 클라이언트 번들용 (빌드 시점에 인라인되므로 next build 전에 설정 필요)
NEXT_PUBLIC_API_BASE_URL=https://api.bitreiver.com
